# images/works/

ロゴデザインなど、静止画で見せたい作品の画像をここに置く。

- ファイル形式は PNG か JPG(背景が透明なロゴは PNG 推奨)
- ファイルを置いたら works.js の該当作品の `image` に
  `"images/works/ファイル名.png"` の形でパスを指定する
- 画像はトリミングされず全体表示されるので、書き出し時の余白は
  そのままデザインの一部として映る(正方形に近いほど収まりが良い)
